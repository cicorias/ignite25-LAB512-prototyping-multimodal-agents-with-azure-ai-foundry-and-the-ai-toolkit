// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
    "name": "Python 3.13 with PostgreSQL",
    "dockerComposeFile": [
        "../docker-compose.yml",
        "docker-compose.devcontainer.yml"
    ],
    "service": "devcontainer",
    "workspaceFolder": "/workspace",
    "features": {
        "ghcr.io/devcontainers/features/azure-cli:1": {
            "extensions": "ml"
        }
        // "ghcr.io/devcontainers/features/dotnet:latest": {
        //     "version": "9.0"
        // }
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "mathematic.vscode-pdf",
                "ms-ossdata.vscode-pgsql",
                "ms-azuretools.vscode-azureresourcegroups",
                "ms-windows-ai-studio.windows-ai-studio",
                "GitHub.copilot"
            ]
        }
    },
    "mounts": [
        // Mount host .azure directory into /workspaces/.azure in the container
        "source=${localWorkspaceFolder}/.azure,target=/workspace/.azure,type=bind"
    ],
    "containerEnv": {
        // Set AZURE_CONFIG_DIR inside container to point to the mounted location
        "AZURE_CONFIG_DIR": "/workspace/.azure"
    }
}